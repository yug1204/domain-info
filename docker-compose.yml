version: '3.8'

services:
  web:
    build: .
    container_name: domain_recon_web
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENVIRONMENT=development
    restart: unless-stopped
